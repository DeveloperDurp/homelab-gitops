apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://gitlab.durp.info/durfy/homelab/gitops.git
    targetRevision: main
    path: infra-talos/external-secrets
  destination:
    namespace: external-secrets
    name: in-cluster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

---

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets-dmz
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://gitlab.durp.info/durfy/homelab/gitops.git
    targetRevision: main
    path: dmz-talos/external-secrets
  destination:
    namespace: external-secrets
    name: dmz
  syncPolicy:
    automated:
      prune: true
      selfHeal: true  
    syncOptions:
      - CreateNamespace=true 

#---
#
#apiVersion: argoproj.io/v1alpha1
#kind: Application
#metadata:
#  name: external-secrets-dev
#  namespace: argocd
#spec:
#  project: default
#  source:
#    repoURL: https://gitlab.durp.info/durfy/homelab/gitops.git
#    targetRevision: main
#    path: dev/external-secrets
#  destination:
#    namespace: external-secrets
#    name: dev
#  syncPolicy:
#    automated:
#      prune: true
#      selfHeal: true
#    syncOptions:
#      - CreateNamespace=true
